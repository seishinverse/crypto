{
  "name": "seishinverse",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/seishinverse/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/seishinverse",
        "main": "apps/seishinverse/src/main.ts",
        "tsConfig": "apps/seishinverse/tsconfig.app.json",
        "assets": ["apps/seishinverse/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/seishinverse/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "seishinverse:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "seishinverse:build:development"
        },
        "production": {
          "buildTarget": "seishinverse:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/seishinverse/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/seishinverse/jest.config.ts"
      }
    }
  },
  "tags": []
}
